#{% set data = load_setup_py_data() %}

package:
  name: ndx-fl-novela
#  version: {{ data.get('version') }}
  version: 0.0.0001

source:
  path: ../ndx-fl-novela

build:
  number: 0

requirements:
  build:
    - python=3.7.4
    - setuptools
  run:
    - python=3.7.4
